<%= form_for(@folder_user) do |f| %>
  <% if @folder_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@folder_user.errors.count, "error") %> prohibited this folder_user from being saved:</h2>

      <ul>
      <% @folder_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :folder_id %><br>
    <%= f.collection_select(:folder_id, Folder.all, :id, :name, {}, class: "form-control")%>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.collection_select(:user_id, User.all, :id, :email, {}, class: "form-control")%>
  </div>

  <br>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
